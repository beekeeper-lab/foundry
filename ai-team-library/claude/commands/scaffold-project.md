---
name: scaffold-project
description: "Creates a new project folder structure from a composition spec — directory tree, CLAUDE.md, agent wrappers, context documents, and output folders. Run this before /compile-team and /seed-tasks."
---

# /scaffold-project

This command is a thin entry point; the canonical process lives in the
`scaffold-project` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/scaffold-project/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
