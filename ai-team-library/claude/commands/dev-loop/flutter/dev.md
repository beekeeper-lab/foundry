---
name: dev
description: "Run the app on the connected device/simulator."
---

# /dev Command

Run the app on the connected device/simulator.

## Usage

```
/dev
```

Runs:

```
flutter run
```

Report the full output. On failure, summarize the first actionable error
and stop — do not retry blindly.
