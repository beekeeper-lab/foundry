---
name: update-docs
description: "Detects documentation drift and proposes updates. Compares docs against recent code, config, and dependency changes to identify stale sections, then proposes specific updates."
---

# /update-docs

This command is a thin entry point; the canonical process lives in the
`update-docs` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/update-docs/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
