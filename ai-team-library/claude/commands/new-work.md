---
name: new-work
description: "Creates a new work item — the single entry point for bugs, features, chores, spikes, and refactors. Asks the right questions, creates a task spec, drafts the appropriate BA artifact (story or bug report), and seeds initial tasks."
---

# /new-work

This command is a thin entry point; the canonical process lives in the
`new-work` skill — single source of truth (SPEC-023). The two used to be
maintained as parallel prose copies and drifted.

Read `.claude/skills/new-work/SKILL.md` and execute its process with these arguments:

$ARGUMENTS
