# Task 02: Tech-QA Verification

| Field | Value |
|-------|-------|
| **Owner** | Tech-QA |
| **Depends On** | 01 |
| **Status** | Done |
| **Started** | 2026-04-17 16:45 |
| **Completed** | 2026-04-17 16:45 |
| **Duration** | < 1m |

## Goal

Independently verify the scaffold additions satisfy the bean's acceptance criteria, that BEAN-242 is now fully green (both assertions), and that no other tests regress.

## Inputs

- Task 01 output: `foundry_app/services/scaffold.py`, `tests/test_scaffold.py`
- `tests/test_generator.py::TestGenerationSelfConsistency` (BEAN-242 driver)
- Acceptance criteria in `bean.md`

## Verification

**Targeted tests:**

```
uv run pytest tests/test_scaffold.py tests/test_generator.py
```

Result: 120 passed.

- `TestGenerationSelfConsistency::test_no_unresolved_jinja_in_generated_files` — PASS (from BEAN-243).
- `TestGenerationSelfConsistency::test_validate_repo_structural_paths_exist` — **PASS** (this bean closes it).
- New `TestCompositionSnapshot` (4 tests) — all green: writes, roundtrips via `load_composition`, idempotent on second pass, re-emits when spec changes.
- New `TestStarterReadme` (2 tests) — all green: writes starter on first run, preserves user-customized README.

**Full suite:**

```
uv run pytest
```

Result: **1800 passed, 0 failed**. No more intentional reds — the BEAN-242 driver is fully green now that both BEAN-243 and BEAN-244 have landed.

**Lint:**

```
uv run ruff check foundry_app/
```

Result: All checks passed.

**Manual regeneration:**

```
uv run foundry-cli generate examples/small-python-team.yml --library ai-team-library --output /tmp/bean244-check
ls /tmp/bean244-check/ai/team/ /tmp/bean244-check/README.md
```

- `/tmp/bean244-check/ai/team/composition.yml` exists and contains the full YAML for project, expertise, team, hooks, generation.
- `/tmp/bean244-check/README.md` exists with project name ("Small Python Team"), pointer to `CLAUDE.md`, structure section, and "Generated by Foundry v1.0.0 on 2026-04-17" footer.
- `grep -rn '{{' /tmp/bean244-check | grep -v '.git/'` → zero matches (placeholder fix from BEAN-243 still holds).

## Acceptance Criteria Check

- [x] Scaffold emits `ai/team/composition.yml` with YAML serialization of the spec (project identity, expertise, team, hooks, generation options all present).
- [x] Scaffold emits `README.md` at project root with name, description, pointer to `CLAUDE.md`, and Foundry footer.
- [x] Unit test verifies composition.yml content (`TestCompositionSnapshot::test_writes_composition_yml` and `test_composition_roundtrip`).
- [x] Unit test verifies README.md content (`TestStarterReadme::test_writes_readme_at_root`).
- [x] Generated project matches `validate-repo` expectations for these two paths.
- [x] BEAN-242 structural assertion passes.
- [x] `uv run pytest` — 1800 passed.
- [x] `uv run ruff check foundry_app/` — clean.

## Definition of Done

- Verification complete. Every AC met. BEAN-242 is fully green.
