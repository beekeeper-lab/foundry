# Task 03 — Developer: Implement Charter Scaffolding

| Field | Value |
|-------|-------|
| **Owner** | Developer |
| **Depends On** | 01, 02 |
| **Status** | Done |
| **Started** | 2026-04-17 18:19 |
| **Completed** | 2026-04-17 18:19 |
| **Duration** | < 1m |

## Goal

Add a `_render_project_charter(spec)` helper to `foundry_app/services/scaffold.py` and emit `ai/context/project-charter.md` from `scaffold_project()` overlay-safely.

## Inputs

- `foundry_app/services/scaffold.py` (target file)
- Task 01 (decision + skeleton)
- Task 02 (per-section content spec)

## Comprehension Note

`scaffold.py` already follows a clean pattern for emitting structured starter files: see `_render_readme(spec)` and the call site that writes `README.md` overlay-safely. The charter emission mirrors that pattern exactly — a private render helper that returns a string, plus a small block at the end of `scaffold_project()` that writes the file only if it does not yet exist and appends to `created` when it does write. `_FOUNDRY_VERSION` and `date.today()` are already imported. No new imports needed.

The `ai/context/` directory is already in `_AI_DIRS` so the parent directory is guaranteed to exist by the time we write the charter.

## Changes

| File | Change |
|------|--------|
| `foundry_app/services/scaffold.py` | Add `_render_project_charter()`; emit `ai/context/project-charter.md` overlay-safely |

## Definition of Done

- [x] `_render_project_charter(spec)` helper added beside `_render_readme(spec)`
- [x] Charter file emitted by `scaffold_project()` only when it does not yet exist
- [x] All five sections present with H2 headings: Purpose, Audience, Success Criteria, Non-Goals, Constraints
- [x] `> **Status:** TODO` admonition at the top of the file
- [x] Description echoed under the H1 (italicized) for both present and absent cases
- [x] Footer mirrors README pattern (`Generated by Foundry vX on YYYY-MM-DD`)
- [x] `created` list includes the charter path when it is written
- [x] `uv run ruff check foundry_app/services/scaffold.py` passes
