# BEAN-252 — Architect Decision Record

| Field | Value |
|-------|-------|
| **Bean** | BEAN-252 |
| **Date** | 2026-04-17 |
| **ADR** | ADR-003 |
| **Outcome** | Charter file (Option 1) |

## Summary

Decision recorded as ADR-003 in `ai/context/decisions.md`. Implementation lands in `foundry_app/services/scaffold.py` as an additive emission step alongside the existing README writer. File path: `ai/context/project-charter.md`. Overlay-safe (do not clobber existing). Five required sections per acceptance criteria.

## Handoff to Developer

- Add `_render_project_charter(spec)` helper next to `_render_readme(spec)` in `scaffold.py`.
- Emit only when the file does not yet exist (mirror README pattern).
- Append the file path to `created` when it is actually written.
- Section order: Purpose → Audience → Success Criteria → Non-Goals → Constraints.
- Echo `spec.project.description` under the title if present; show a `TODO` line if not.
- Footer must mirror README: `Generated by Foundry vX on YYYY-MM-DD`.
