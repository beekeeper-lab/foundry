# BEAN-261 Verification — Tech-QA

| Field | Value |
|-------|-------|
| **Bean** | BEAN-261 — Propagate Missing-Expertise Drop to Agent Headers and Member Files |
| **Verifier** | tech-qa |
| **Date** | 2026-04-17 |
| **Outcome** | Pass |

## Scope

Verify that a missing-source expertise (one whose `conventions.md` is
absent from the library) does not appear in any compiled artifact
generated by Foundry — not just `CLAUDE.md` (BEAN-247) but also:

- `.claude/agents/<persona>.md` headers
- `ai/generated/members/<persona>.md` expertise-context substitutions

Also confirm the warning channel is preserved and the composition.yml
decision is recorded.

## Checks

### 1. Automated test suite

- `uv run pytest` → **1813 passed, 4 warnings** (pre-existing Qt
  deprecation warnings unrelated to this bean).
- `uv run ruff check foundry_app/` → **All checks passed**.

### 2. New tests land and cover the invariants

- `tests/test_agent_writer.py::TestWriteAgents::test_missing_expertise_excluded_from_agent_header`
  — asserts `clean-code` is absent from the agent header when its
  `conventions.md` is missing; present expertise still renders; role
  name derives from the emitted-expertise list.
- `tests/test_compiler.py::TestCompileWarnings::test_missing_expertise_excluded_from_member_file`
  — asserts `{{ expertise | join(", ") }}` substitution in
  `ai/generated/members/<persona>.md` excludes missing-source IDs.
- Pre-existing `test_missing_expertise_excluded_from_claude_md` still
  passes — CLAUDE.md invariant (BEAN-247) intact.

### 3. Manual regeneration

`uv run foundry-cli generate examples/small-python-team.yml --library
ai-team-library --output /tmp/foundry-gen-check-bean261 --force`

Post-generation grep for `clean-code`:

- `.claude/agents/*.md` → **0 hits** across 4 agent files.
- `ai/generated/members/*.md` → **0 hits** across 4 member files.
- `CLAUDE.md` → **0 hits**.
- `ai/team/composition.yml` → **1 hit** (`- id: clean-code`). This is
  intentional per the bean Notes decision: composition.yml records the
  user's input spec, not the emitted-artifact set.

The generator also emitted the expected warning via
`StageResult.warnings`: `Expertise 'clean-code' missing conventions.md`.

### 4. composition.yml decision

Documented in `bean.md` → Notes → "Decision — composition.yml". The
spec stays unfiltered; the missing expertise is observable via the
warning path and the absence of `ai/generated/expertise/<id>.md`.

## Conclusion

All acceptance criteria are met. The missing-expertise drop propagates
consistently to every compiled artifact that previously referenced it.
The warning path is preserved, and `composition.yml` intentionally
retains the user's input for round-trippability.
